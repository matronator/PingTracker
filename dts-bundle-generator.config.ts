const config = {
    entries: [
        {
            filePath: "./src/index.ts",
            outFile: "./dist/ping-tracker.d.ts",
            noCheck: false,
        },
    ],
};

module.exports = config;
